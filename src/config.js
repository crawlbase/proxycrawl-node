const config = {
  defaults: {
    timeout: 30000,
    postContentType: 'application/x-www-form-urlencoded'
  },
  test: {
    normalToken: '',
    javascriptToken: ''
  }
};

module.exports = config;
